change: uwsm-adoption
status: ready_to_archive
current_phase: archive
phases:
  explore:   { status: complete }
  proposal:  { status: complete }
  spec:      { status: complete }
  design:    { status: complete }
  tasks:     { status: complete }
  apply:     { status: complete }
  verify:
    status: complete
    static: pass
    runtime: pass
    critical: 0
    warnings_resolved: 1
    suggestions: 3
    pending_visual: [5.6 AGS bar visual identity]
    pending_future: [5.8 logout latency under 5s — observe on next natural logout]
  archive:   { status: pending }
artifact_store: hybrid
delivery_strategy: ask-on-risk
chain_strategy: stacked-to-main
sequencing:
  must_ship_before: desktop-redesign-slice-a
  reason: |
    desktop-redesign Slice D removes swaync. If uwsm-adoption ships after
    desktop-redesign, the autostart wrapping has to be done twice. Per
    proposal + design ADR-4, uwsm-adoption must complete apply + verify
    BEFORE the agent resumes desktop-redesign apply.
notes: |
  Planning artifacts written on the VirtualBox VM session before the
  user's migration to bare-metal CachyOS. Apply was DELIBERATELY skipped
  on the VM because:
    - VBox virtio-gpu has applyCommit issues with aquamarine that would
      mask any uwsm-related modeset behavior.
    - SDDM session selection on the VM is irrelevant — the user is moving
      to a new install.
    - uwsm session lifecycle (logout/login + service activation timing)
      must be tested on the real target host.

  Apply path (bare-metal):
    1. Phases 1-4 implemented in commit 6bd4810.
    2. Runtime verification found CRITICAL drop-in bug — original
       [Autologin]-only form was ignored by SDDM for manual login.
       Fixed in 9e2538d (two-section form with [General] DefaultSession=
       + [Autologin] Session=). Spec, design (ADR-3), tasks (3.1, 5.10),
       and apply-progress updated in the same commit.
    3. User re-ran install.sh, which detected stale content and rewrote
       the drop-in to the two-section form. User then logged out + back
       in via SDDM into the uwsm-managed session and ran the runtime
       checklist.
    4. Final verify (this pass): 9 PASS, 2 PASS-INFERRED, 1 PENDING-VISUAL
       (5.6 AGS bar), 1 PENDING-FUTURE (5.8 logout latency). No CRITICAL.
       W1 RESOLVED. SUGGESTIONS S1/S2/S3 recorded as non-blocking.
    5. ARCHIVE-READY. Next: sdd-archive uwsm-adoption. Then resume
       desktop-redesign Slice A.

  Optional close-out at archive: ask user one yes/no on AGS bar visual
  identity to flip 5.6 from PENDING-VISUAL to PASS.
estimated_changed_lines: 63
estimated_files_touched: 3
chained_prs_recommended: false
