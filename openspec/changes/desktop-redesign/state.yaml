change: desktop-redesign
status: in_progress
current_phase: apply
phases:
  explore:   { status: complete }
  proposal:  { status: complete }
  spec:      { status: complete }
  design:    { status: complete }
  tasks:     { status: complete }
  apply:     { status: in_progress, completed_slices: [A0], pending_slices: [A, B, C, D] }
  verify:    { status: pending }
  archive:   { status: pending }
artifact_store: hybrid
delivery_strategy: ask-on-risk
chain_strategy: stacked-to-main
sequencing: {}
notes: |
  Released 2026-05-27 — uwsm-adoption archived (session-management capability complete).
  Blocked-by constraint lifted. Resume order remains:
    1. Smoke-test Slice A0 (config/ags/service/hyprland.ts polls
       Hyprland Socket2 — confirm activeWorkspace updates when switching
       workspaces; activeTitle updates when focusing a window).
    2. sdd-continue (Slice A: BarShell + Bar Aurora + Workspaces + fonts).
    3. Continue through Slices B, C, D as stacked PRs to main.
