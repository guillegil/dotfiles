# pacman.txt — official-repo packages (Arch / CachyOS)
# One package per line. Lines starting with # and blank lines are ignored.
# Install all:  grep -vE '^\s*(#|$)' pacman.txt | xargs -r sudo pacman -S --needed

# --- Hyprland compositor + first-party ecosystem ---------------------------
hyprland
hyprpaper
hyprlock
hypridle
hyprpicker
xdg-desktop-portal-hyprland
uwsm

# --- Display manager -------------------------------------------------------
sddm                        # login screen; enabled by install.sh

# --- Core desktop pieces ---------------------------------------------------
kitty                       # terminal
# swaync removed — AGS (AstalNotifd) is the notification daemon. swaync's
# systemd user unit is Type=dbus on org.freedesktop.Notifications, so leaving
# it installed lets D-Bus re-activate it and steal the bus from notifd.
hyprpolkitagent             # polkit agent; enabled by install.sh
thunar                      # file manager

# --- Screenshot / clipboard ------------------------------------------------
grim
slurp
wl-clipboard
cliphist

# --- Audio (PipeWire stack) ------------------------------------------------
pipewire
pipewire-pulse
wireplumber

# --- Fonts -----------------------------------------------------------------
ttf-jetbrains-mono-nerd
ttf-ibm-plex                # IBM Plex Sans (popover prose font)

# --- System utilities ------------------------------------------------------
openssh                     # SSH access to the machine
brightnessctl               # backlight control (multimedia keys)
playerctl                   # media key control

# --- AGS build dependency --------------------------------------------------
dart-sass                   # 'sass' binary; AGS compiles SCSS with it

# --- VM guest integration --------------------------------------------------
# Uncomment whichever matches your hypervisor when running this dotfiles
# inside a VM. All three are no-ops on bare-metal so they can be left
# commented in the default checkout.
# virtualbox-guest-utils   # VirtualBox + VBoxClient
# qemu-guest-agent         # QEMU/KVM
# spice-vdagent            # SPICE channel (used with qemu/virt-manager)
